# Hundtracker Backend - Environment variables
# Copy to .env and fill in values

# Database (default in docker-compose)
DATABASE_URL=postgresql+asyncpg://tracker:tracker@localhost:5432/hundtracker

# MQTT (from mqtt settings.txt)
MQTT_HOST=mqtt.chickendinner.vip
MQTT_PORT=8883
MQTT_USERNAME=trackerbackend
MQTT_PASSWORD=x6w8rgaT2

# JWT - generate with: openssl rand -hex 32
JWT_SECRET=your-secret-key-minimum-32-chars
